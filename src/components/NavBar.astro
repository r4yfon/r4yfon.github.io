<nav class="container mx-auto my-6 flex items-center justify-between px-6 py-4 bg-white dark:bg-gray-900 shadow rounded-xl sticky top-4 z-50">
  <div class="text-2xl font-bold text-gray-900 dark:text-white">
    My Portfolio
  </div>
  <ul class="flex space-x-6">
    <li>
      <a href="#projects" class="text-gray-700 dark:text-gray-200 hover:underline">Projects</a>
    </li>
    <li>
      <a href="#contact" class="text-gray-700 dark:text-gray-200 hover:underline">Contact</a>
    </li>
  </ul>
  <label class="toggle-button">
    <input type="checkbox" id="dark" />
    <span>Toggle Dark Mode</span>
  </label>
</nav>

<style>
  /* Hide the native checkbox */
.toggle-button input[type="checkbox"] {
  position: absolute;
  opacity: 0;
  width: 0;
  height: 0;
  pointer-events: none;
}

/* Style the label to look like a button */
.toggle-button{
  display: inline-block;
  padding: 0.5em 1.2em;
  background-color: #eee;
  border: 2px solid #ccc;
  border-radius: 6px;
  cursor: pointer;
  user-select: none;
  transition: background-color 0.3s, border-color 0.3s;
}

/* Change appearance when checked */
.toggle-button:has(input[type="checkbox"]:checked) {
  background-color: #333;
  color: white;
  border-color: #333;
}
</style>